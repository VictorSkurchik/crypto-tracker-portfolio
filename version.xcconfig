// Single source of truth for the app's version across Android, Desktop, and iOS — included
// directly by iosApp/Configuration/Config.xcconfig and parsed by the root build.gradle.kts.
// CURRENT_PROJECT_VERSION must stay strictly increasing (Play Store enforces this for versionCode).
MARKETING_VERSION = 1.0.0
CURRENT_PROJECT_VERSION = 1
